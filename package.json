{
  "name": "sullana-noticias",
  "version": "0.1.0",
  "private": true,
  "description": "MVP editorial de noticias locales para Sullana, Piura.",
  "type": "module",
  "scripts": {
    "build": "bash scripts/build-site.sh",
    "site:validate": "npm run build && node --check dist/server/index.js",
    "start": "node server.js",
    "dev": "node --watch server.js",
    "seed": "node scripts/seed-demo.js",
    "ingest": "node scripts/run-ingest.js",
    "test": "node --test",
    "check": "node --check server.js && node --check worker/index.js && node --check scripts/seed-demo.js && node --check scripts/run-ingest.js"
  },
  "engines": {
    "node": ">=22.5"
  },
  "license": "MIT"
}
